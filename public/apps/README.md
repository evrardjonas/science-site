# Applications autonomes

Chaque jeu ou animation HTML autonome peut être placé dans son propre dossier :

```text
public/apps/nom-de-l-application/
├── index.html
├── style.css
├── script.js
└── assets/
```

Il peut ensuite être affiché avec le composant `InteractiveFrame.astro`.
